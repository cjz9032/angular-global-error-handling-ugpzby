import { AppEventDirective } from './app-event.directive';

describe('DocumentEventDirective', () => {
	it('should create an instance', () => {
		// const directive = new AppEventDirective();
		// expect(directive).toBeTruthy();
	});
});

import { PunctuationRemoveSpacePipe } from './punctuation-remove-space.pipe';

describe('PunctuationRemoveSpacePipe', () => {
	it('create an instance', () => {
		const pipe = new PunctuationRemoveSpacePipe();
		expect(pipe).toBeTruthy();
	});
});

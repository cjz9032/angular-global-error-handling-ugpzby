import { SanitizePipe } from './sanitize.pipe';
import { DomSanitizer } from '@angular/platform-browser';

describe('SanitizePipe', () => {
	it('create an instance', () => {
		// const pipe = new SanitizePipe(new DomSanitizer());
		// expect(pipe).toBeTruthy();
	});
});

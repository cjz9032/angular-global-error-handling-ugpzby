import { PipeInstallPipe } from './pipe-install.pipe';

describe('PipeInstallPipe', () => {
	// it('create an instance', () => {
	//   const pipe = new PipeInstallPipe();
	//   expect(pipe).toBeTruthy();
	// });
});

import { ChoosePagePipe } from './choose-page.pipe';

describe('ChoosePagePipe', () => {
  it('create an instance', () => {
    const pipe = new ChoosePagePipe();
    expect(pipe).toBeTruthy();
  });
});

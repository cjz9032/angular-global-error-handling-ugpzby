import { SvgInlinePipe } from './svg-inline.pipe';

describe('SvgInlinePipe', () => {
	// it('create an instance', () => {
	//   const pipe = new SvgInlinePipe();
	//   expect(pipe).toBeTruthy();
	// });
});

import { SanitizehtmlpipePipe } from './sanitizehtml.pipe';

describe('SanitizehtmlpipePipe', () => {
	it('create an instance', () => {
		const pipe = new SanitizehtmlpipePipe();
		expect(pipe).toBeTruthy();
	});
});

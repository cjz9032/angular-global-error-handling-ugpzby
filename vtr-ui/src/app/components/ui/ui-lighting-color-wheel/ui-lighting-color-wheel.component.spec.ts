import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { UiLightingColorWheelComponent } from './ui-lighting-color-wheel.component';

describe('UiLightingColorWheelComponent', () => {
//   let component: UiLightingColorWheelComponent;
//   let fixture: ComponentFixture<UiLightingColorWheelComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ UiLightingColorWheelComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(UiLightingColorWheelComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
   // expect(component).toBeTruthy();
  });
});

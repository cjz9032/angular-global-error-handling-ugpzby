import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ModalSupportWechatComponent } from './modal-support-wechat.component';

describe('ModalSupportWechatComponent', () => {
//   let component: ModalSupportWechatComponent;
//   let fixture: ComponentFixture<ModalSupportWechatComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ ModalSupportWechatComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(ModalSupportWechatComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});

import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ModalWifiSecuriryLocationNoticeComponent } from './modal-wifi-securiry-location-notice.component';

describe('ModalWifiSecuriryLocationNoticeComponent', () => {
	// let component: ModalWifiSecuriryLocationNoticeComponent;
	// let fixture: ComponentFixture<ModalWifiSecuriryLocationNoticeComponent>;

	beforeEach(async(() => {
		// TestBed.configureTestingModule({
		// 	declarations: [ModalWifiSecuriryLocationNoticeComponent]
		// })
		// 	.compileComponents();
	}));

	beforeEach(() => {
		// fixture = TestBed.createComponent(ModalWifiSecuriryLocationNoticeComponent);
		// component = fixture.componentInstance;
		// fixture.detectChanges();
	});

	it('should create', () => {
		// expect(component).toBeTruthy();
	});
});

import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ConnectedHomeMyHomeComponent } from './connected-home-my-home.component';

describe('ConnectedHomeMyHomeComponent', () => {
	// let component: ConnectedHomeMyHomeComponent;
	// let fixture: ComponentFixture<ConnectedHomeMyHomeComponent>;

	beforeEach(async(() => {
		// TestBed.configureTestingModule({
		// 	declarations: [ConnectedHomeMyHomeComponent]
		// })
		// 	.compileComponents();
	}));

	beforeEach(() => {
		// fixture = TestBed.createComponent(ConnectedHomeMyHomeComponent);
		// component = fixture.componentInstance;
		// fixture.detectChanges();
	});

	it('should create', () => {
		// expect(component).toBeTruthy();
	});
});

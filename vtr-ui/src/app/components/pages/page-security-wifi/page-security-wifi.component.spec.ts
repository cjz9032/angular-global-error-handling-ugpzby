import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { PageSecurityWifiComponent } from './page-security-wifi.component';

describe('PageSecurityWifiComponent', () => {
	// let component: PageSecurityWifiComponent;
	// let fixture: ComponentFixture<PageSecurityWifiComponent>;

	beforeEach(waitForAsync(() => {
		// TestBed.configureTestingModule({
		// 	declarations: [PageSecurityWifiComponent]
		// })
		// 	.compileComponents();
	}));

	beforeEach(() => {
		// fixture = TestBed.createComponent(PageSecurityWifiComponent);
		// component = fixture.componentInstance;
		// fixture.detectChanges();
	});

	it('should create', () => {
		// expect(component).toBeTruthy();
	});
});

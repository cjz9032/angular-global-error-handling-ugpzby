import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PageSecurityHomeSecurityComponent } from './page-security-home-security.component';

describe('PageSecurityHomeSecurityComponent', () => {
	// let component: PageSecurityHomeSecurityComponent;
	// let fixture: ComponentFixture<PageSecurityHomeSecurityComponent>;

	beforeEach(async(() => {
		// TestBed.configureTestingModule({
		// 	declarations: [ PageSecurityHomeSecurityComponent ]
		// })
		// .compileComponents();
	}));

	beforeEach(() => {
		// fixture = TestBed.createComponent(PageSecurityHomeSecurityComponent);
		// component = fixture.componentInstance;
		// fixture.detectChanges();
	});

	it('should create', () => {
		// expect(component).toBeTruthy();
	});
});

import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AdvisorWifiSecurityComponent } from './advisor-wifi-security.component';

describe('AdvisorWifiSecurityComponent', () => {
	// let component: AdvisorWifiSecurityComponent;
	// let fixture: ComponentFixture<AdvisorWifiSecurityComponent>;

	beforeEach(async(() => {
		// TestBed.configureTestingModule({
		// 	declarations: [AdvisorWifiSecurityComponent]
		// })
		// 	.compileComponents();
	}));

	beforeEach(() => {
		// fixture = TestBed.createComponent(AdvisorWifiSecurityComponent);
		// component = fixture.componentInstance;
		// fixture.detectChanges();
	});

	it('should create', () => {
		// expect(component).toBeTruthy();
	});
});

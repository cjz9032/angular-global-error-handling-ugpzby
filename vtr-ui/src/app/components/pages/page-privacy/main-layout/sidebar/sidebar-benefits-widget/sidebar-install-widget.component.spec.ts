import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SidebarInstallWidgetComponent } from './sidebar-install-widget.component';

describe('SidebarInstallWidgetComponent', () => {
  // let component: SidebarInstallWidgetComponent;
  // let fixture: ComponentFixture<SidebarInstallWidgetComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ SidebarInstallWidgetComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(SidebarInstallWidgetComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });

  it('should be false', () => {
    // expect(component.isOpen).toEqual(false);
  })
});

import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OneClickScanComponent } from './one-click-scan.component';

describe('Permit', () => {
	// let component: OneClickScanComponent;
	// let fixture: ComponentFixture<OneClickScanComponent>;

	beforeEach(async(() => {
		// TestBed.configureTestingModule({
		// 	declarations: [OneClickScanComponent]
		// })
		// 	.compileComponents();
	}));

	beforeEach(() => {
		// fixture = TestBed.createComponent(OneClickScanComponent);
		// component = fixture.componentInstance;
		// fixture.detectChanges();
	});

	it('should create', () => {
		// expect(component).toBeTruthy();
	});
});

import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { NonPrivatePasswordComponent } from './non-private-password.component';

describe('NonPrivatePasswordComponent', () => {
  // let component: NonPrivatePasswordComponent;
  // let fixture: ComponentFixture<NonPrivatePasswordComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ NonPrivatePasswordComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(NonPrivatePasswordComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});

import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ScanStepsComponent } from './scan-steps.component';

describe('ScanStepsComponent', () => {
  // let component: ScanStepsComponent;
  // let fixture: ComponentFixture<ScanStepsComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ ScanStepsComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(ScanStepsComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});

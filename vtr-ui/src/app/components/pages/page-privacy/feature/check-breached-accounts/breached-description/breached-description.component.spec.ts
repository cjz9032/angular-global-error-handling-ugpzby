import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BreachedDescriptionComponent } from './breached-description.component';

describe('BreachedDescriptionComponent', () => {
  // let component: BreachedDescriptionComponent;
  // let fixture: ComponentFixture<BreachedDescriptionComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ BreachedDescriptionComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(BreachedDescriptionComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});

import { SitesFaviconPipe } from './sites-favicon.pipe';

describe('SitesFaviconPipe', () => {
  it('create an instance', () => {
    // const pipe = new SitesFaviconPipe();
    // expect(pipe).toBeTruthy();
  });
});

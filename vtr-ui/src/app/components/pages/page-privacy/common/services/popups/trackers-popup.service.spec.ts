import { TestBed } from '@angular/core/testing';

import { TrackersPopupService } from './trackers-popup.service';

describe('TrackersPopupService', () => {
  //beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    // const service: TrackersPopupService = TestBed.get(TrackersPopupService);
    // expect(service).toBeTruthy();
  });
});

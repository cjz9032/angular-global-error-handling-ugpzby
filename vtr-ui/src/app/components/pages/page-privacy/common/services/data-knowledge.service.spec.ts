import { TestBed } from '@angular/core/testing';

import { DataKnowledgeService } from './data-knowledge.service';

describe('DataKnowledgeService', () => {
  //beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    // const service: DataKnowledgeService = TestBed.get(DataKnowledgeService);
    // expect(service).toBeTruthy();
  });
});

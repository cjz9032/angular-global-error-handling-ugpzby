import { TestBed } from '@angular/core/testing';

import { CommunicationWithFigleafService } from './communication-with-figleaf.service';

describe('CommunicationWithFigleafService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    // const service: CommunicationWithFigleafService = TestBed.get(CommunicationWithFigleafService);
    // expect(service).toBeTruthy();
  });
});

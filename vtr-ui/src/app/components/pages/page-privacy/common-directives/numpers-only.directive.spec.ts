import { NumpersOnlyDirective } from './numpers-only.directive';

describe('NumpersOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumpersOnlyDirective();
    expect(directive).toBeTruthy();
  });
});

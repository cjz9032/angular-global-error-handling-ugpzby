import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PageSecurityWindowsHelloComponent } from './page-security-windows-hello.component';

describe('PageSecurityWindowsHelloComponent', () => {
  // let component: PageSecurityWindowsHelloComponent;
  // let fixture: ComponentFixture<PageSecurityWindowsHelloComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ PageSecurityWindowsHelloComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(PageSecurityWindowsHelloComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});

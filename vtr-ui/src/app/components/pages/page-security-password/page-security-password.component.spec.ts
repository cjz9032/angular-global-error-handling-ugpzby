import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { PageSecurityPasswordComponent } from './page-security-password.component';

describe('PageSecurityPasswordComponent', () => {
	// let component: PageSecurityPasswordComponent;
	// let fixture: ComponentFixture<PageSecurityPasswordComponent>;

	beforeEach(waitForAsync(() => {
		// TestBed.configureTestingModule({
		// 	declarations: [PageSecurityPasswordComponent]
		// })
		// 	.compileComponents();
	}));

	beforeEach(() => {
		// fixture = TestBed.createComponent(PageSecurityPasswordComponent);
		// component = fixture.componentInstance;
		// fixture.detectChanges();
	});

	it('should create', () => {
		// expect(component).toBeTruthy();
	});
});

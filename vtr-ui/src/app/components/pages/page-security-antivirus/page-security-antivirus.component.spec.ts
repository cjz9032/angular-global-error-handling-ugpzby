import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { PageSecurityAntivirusComponent } from './page-security-antivirus.component';

describe('PageSecurityAntivirusComponent', () => {
	// let component: PageSecurityAntivirusComponent;
	// let fixture: ComponentFixture<PageSecurityAntivirusComponent>;

	beforeEach(waitForAsync(() => {
		// TestBed.configureTestingModule({
		// 	declarations: [PageSecurityAntivirusComponent]
		// })
		// 	.compileComponents();
	}));

	beforeEach(() => {
		// fixture = TestBed.createComponent(PageSecurityAntivirusComponent);
		// component = fixture.componentInstance;
		// fixture.detectChanges();
	});

	it('should create', () => {
		// expect(component).toBeTruthy();
	});
});
